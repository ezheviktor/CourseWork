<Window x:Class="SnakeGame.DifficultyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SnakeGame"
        mc:Ignorable="d"
        Title="Difficulty" Height="170" Width="250"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        
    </Window.Resources>
    <Border Margin="10">
        <Grid >
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
            </Grid.RowDefinitions>

            <StackPanel Name="DifficultyOptions" Margin="10" Orientation="Vertical" Grid.Row="0">
                <RadioButton  Content="Hard"></RadioButton>
                <RadioButton Content="Medium"></RadioButton>
                <RadioButton Content="Easy"></RadioButton>
            </StackPanel>
            <WrapPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" >
                <Button IsCancel="True" MinWidth="75" Margin="0,10,10,0">Cancel</Button>
                <Button IsDefault="True" Click="SetDifficulty_Click" MinWidth="75" Margin="0,10,10,0">Set</Button>
            </WrapPanel>
        </Grid>
    </Border>

    
</Window>
